//This program solve the expression and print the values in console.

#include<stdio.h>

int main()
{
    int z = ((x+3)*(x*x*x))/((y-4)*(y+5));

    int r = (2v + (6.22 * (c + d)))/(g + v);

    int A = (7.7b * (xy + a)) / ((c - 0.8)+2b);

    int X = ((12 * (x * x *))/4x) + ((8 * (x * x))/4x) + (x / (8 * x)) + (8/(8 * x));

    return 0;
}
