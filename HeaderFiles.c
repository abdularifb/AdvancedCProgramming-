#include<stdio.h>
#include "other.h"
int main(){
    //printf("%d",reversenumber(143));
    int n=1441;
    if(temp==reversenumber(n)){
        printf("%d is Palindrome\n",n);
    }
    else{
        printf("%d is Not Palindrome",n);
    }
    return 0;
}
